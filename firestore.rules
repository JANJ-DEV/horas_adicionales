rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny-all. Tighten rules per collection before production deploy.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
